#include<stdio.h>
int main()
{
	int n,r,temp,sum=0;
	printf("enter the num:");
	scanf("%d",&n);
	temp=n;
	while(n>0)
	{
		r=n%10;
		sum+=(r*r*r);
		n=n/10;
	}
	if(temp==sum)
	{
		printf("arm");
	}
	else
	{
		printf("not arm");
	}
}
